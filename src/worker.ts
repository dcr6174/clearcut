/// <reference lib="webworker" />import{pipeline,env,RawImage}from'@huggingface/transformers';env.allowLocalModels=false;env.useBrowserCache=true;let remover:any;const cancelled=new Set<number>();self.onmessage=async(e:MessageEvent)=>{const{id,type}=e.data;if(type==='cancel'){cancelled.add(id);return}try{postMessage({id,type:'progress',progress:{status:'loading',progress:4,message:'Preparing local AI'}});if(!remover){try{remover=await pipeline('background-removal','Xenova/modnet',{dtype:'fp32',progress_callback:(x:any)=>postMessage({id,type:'progress',progress:{status:'loading',progress:Math.round(x.progress??12),message:x.status==='progress'?'Downloading model once':'Loading model'}})})}catch{remover=await pipeline('background-removal','Xenova/modnet',{dtype:'fp32'})}}if(cancelled.has(id))throw new DOMException('Cancelled','AbortError');postMessage({id,type:'progress',progress:{status:'processing',progress:76,message:'Separating subject'}});const url=URL.createObjectURL(new Blob([e.data.bytes],{type:e.data.mime}));const image=await RawImage.fromURL(url);URL.revokeObjectURL(url);const out=await remover([image]);const cutout=(Array.isArray(out)?out[0]:out);const canvas=new OffscreenCanvas(cutout.width,cutout.height);const ctx=canvas.getContext('2d')!;ctx.drawImage(cutout.toCanvas(),0,0);const blob=await canvas.convertToBlob({type:'image/png'});const buffer=await blob.arrayBuffer();postMessage({id,type:'result',buffer},[buffer])}catch(err:any){postMessage({id,type:'error',message:err?.message||'Processing failed'})}finally{cancelled.delete(id)}};
